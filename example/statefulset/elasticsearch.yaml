apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: kubedb-elasticsearch-demo
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      kubedb.com/type: elasticsearch
      elastic.kubedb.com/name: elasticsearch-demo
  serviceName: governing-elasticsearch
  template:
    metadata:
      annotations:
        pod.beta.kubernetes.io/init-containers: '[
          {
            "name": "discover",
            "image": "kubedb/es-operator:canary",
            "imagePullPolicy": "IfNotPresent",
            "args": ["discover", "--service=elasticsearch-demo"],
            "env": [
              {
                "name": "POD_NAME",
                "valueFrom": {
                  "fieldRef": {
                    "apiVersion": "v1",
                    "fieldPath": "metadata.name"
                  }
                }
              }
            ],
            "volumeMounts": [
              {
                "name": "discovery",
                "mountPath": "/tmp/discovery"
              }
            ]
          }
        ]'
      labels:
        kubedb.com/type: elasticsearch
        elastic.kubedb.com/name: elasticsearch-demo
    spec:
      containers:
      - image: kubedb/elasticsearch:canary
        imagePullPolicy: IfNotPresent
        name: elasticsearch
        env:
        - name: CLUSTER_NAME
          value: elasticsearch-demo
        ports:
        - containerPort: 9200
          name: api
          protocol: TCP
        - containerPort: 9300
          name: tcp
          protocol: TCP
        volumeMounts:
        - name: discovery
          mountPath: /tmp/discovery
        - name: volume
          mountPath: /var/pv
      volumes:
      - name: discovery
        emptyDir: {}
      - name: volume
        emptyDir: {}
